List<account> accList = new list<account>();
for (Integer i = 0; i < 5; i++) {
    account a = new account();
    a.Name = 'Trig July v'+i;

    accList.add(a);
    //insert a;
}
insert accList; //# of DML --> 1.